{
  "name": "tlao",
  "version": "1.3.24",
  "description": "TLÁO - Tactical Layer for Autonomous Operations - AWS 10,000 AIdeas Competition Entry",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:landing": "pnpm --filter @tlao/landing dev",
    "dev:docs": "pnpm --filter @tlao/docs dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean",
    "version:patch": "pnpm run privacy:check && versioning patch",
    "version:minor": "pnpm run privacy:check && versioning minor",
    "version:major": "pnpm run privacy:check && versioning major",
    "version:prerelease": "pnpm run privacy:check && versioning bump prerelease",
    "version:sync": "versioning sync",
    "version:changelog": "versioning changelog",
    "version:validate": "versioning validate",
    "version:cleanup": "versioning cleanup",
    "version:reentry": "versioning reentry",
    "version:roadmap": "versioning roadmap",
    "env:sync": "versioning env sync",
    "env:doctor": "versioning env doctor",
    "env:validate": "versioning env validate --all",
    "version:secrets": "versioning check-secrets",
    "security:scan": "bash scripts/security-scan.sh",
    "security:scan:staged": "bash scripts/security-scan.sh --staged",
    "privacy:check": "bash scripts/privacy-guard.sh --staged",
    "privacy:release": "git fetch --quiet origin main && bash scripts/privacy-guard.sh --range \"$(git merge-base origin/main HEAD)..HEAD\"",
    "test:privacy": "python3 -m unittest tests/test_privacy_guard.py tests/test_release_privacy_wiring.py",
    "prerelease": "pnpm run version:validate && pnpm run version:secrets && pnpm run privacy:release && pnpm run test:privacy",
    "postrelease": "pnpm run version:changelog && pnpm run version:sync",
    "push:release": "pnpm run privacy:release && git push && git push --tags",
    "release": "pnpm run prerelease && pnpm run lint && pnpm run type-check && pnpm run build && pnpm run postrelease && pnpm run push:release",
    "release:patch": "pnpm run version:patch && pnpm run release",
    "release:minor": "pnpm run version:minor && pnpm run release",
    "release:major": "pnpm run version:major && pnpm run release",
    "release:prerelease": "pnpm run version:prerelease && pnpm run release",
    "release:landing": "pnpm run prerelease && pnpm --filter @tlao/landing release && pnpm run postrelease && pnpm run push:release",
    "release:docs": "pnpm run prerelease && pnpm --filter @tlao/docs release && pnpm run postrelease && pnpm run push:release",
    "release:backend": "pnpm --filter @tlao/backend release && pnpm run push:release",
    "release:landing:patch": "pnpm run version:patch && pnpm run release:landing",
    "release:landing:minor": "pnpm run version:minor && pnpm run release:landing",
    "release:landing:major": "pnpm run version:major && pnpm run release:landing",
    "release:docs:patch": "pnpm run version:patch && pnpm run release:docs",
    "release:docs:minor": "pnpm run version:minor && pnpm run release:docs",
    "release:docs:major": "pnpm run version:major && pnpm run release:docs",
    "release:backend:patch": "pnpm --filter @tlao/backend release",
    "release:backend:minor": "pnpm --filter @tlao/backend release",
    "release:backend:major": "pnpm --filter @tlao/backend release",
    "prepare": "husky",
    "pre-commit": "pnpm run version:validate && pnpm exec lint-staged && pnpm run privacy:check && pnpm run test:privacy && pnpm run version:secrets && pnpm run security:scan:staged"
  },
  "devDependencies": {
    "@edcalderon/versioning": "^1.5.9",
    "eslint": "^8.57.1",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.0",
    "prettier": "^3.7.4",
    "turbo": "^1.13.4"
  },
  "packageManager": "pnpm@8.12.0",
  "engines": {
    "node": "22.22.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "framer-motion": "^12.23.26"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yaml,yml}": [
      "prettier --write"
    ],
    "package.json": [
      "pnpm run version:validate"
    ]
  }
}
